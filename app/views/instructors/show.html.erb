<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>

    <link rel="stylesheet" type="text/css" href="instructors.scss">


  </head>
  <body>


    <div class = "alltext">
    <div class="row">
      <div class="col-12">
          <nav>
            <a href="/instructors/<%=session[:instructor_id]%>">VIEW PROFILE</a>
            <%=link_to "LOGOUT", logout_path, method: "delete", data: { confirm: 'Are you sure you want to logout?' } %>
          </nav>
      </div>
    </div>
    </div>



<div class="card" id="student_profile">
  <div class ="col-4">
<h1> Welcome <%= @instructor.name %> </h1>
</div>
</div>

<div class="card" id="student_profile">
  <div class ="col-4">
    <br>
<b> Your Information: </b><br>
<%= "Years of Experience: #{@instructor.experience} years" %> <br>
<%= "Hourly Rate: $#{@instructor.rate}" %><br><br>

<b> Your Mountains: </b><br>
  <% @instructor.mountains.each do|mountain| %>
    <%= mountain.name %><br>
  <% end %>
<br>

  <% if @instructor == Instructor.find_by({id: session[:instructor_id]})%>
    <%= button_to 'Edit', edit_instructor_path, method: :get, data: { confirm: 'Are you sure you want to edit?' } %>
    <%= button_to 'Log Out', logout_path, method: :delete, data: { confirm: 'are u sure lol' } %>
    <br>
  <%end%>

</div>
</div>


<div class="card" id="student_profile">
  <div class ="col-4">
<h3> Your Upcoming Bookings </h3>
<% @instructor.upcoming_bookings.each do |book| %>
<%= "Appointment Time: #{book.format_time}" %><br>
<%= "Hours: #{book.duration}" %><br>
---<br>
<%= "Student: #{book.student.name}" %><br>
<%= "Student Age: #{book.student.age}" %><br>
<%= "Student Level: #{book.student.level}" %><br>
--<br>
<%= "Mountain: #{book.mountain_instructor.mountain.name}" %><br>
<%= "Mountain Location: #{book.mountain_instructor.mountain.location}" %><br>
--
<% end %>
</div>
</div>

<div class="card" id="student_profile">
  <div class ="col-4">
<h3> Your Previous Bookings </h3>

<% @instructor.previous_bookings.each do |book| %>
<%= "Appointment Time: #{book.format_time}" %><br>
<%= "Hours: #{book.duration}" %><br>
---<br>
<%= "Student: #{book.student.name}" %><br>
<%= "Student Age: #{book.student.age}" %><br>
<%= "Student Level: #{book.student.level}" %><br>
--<br>
<%= "Mountain: #{book.mountain_instructor.mountain.name}" %><br>
<%= "Mountain Location: #{book.mountain_instructor.mountain.location}" %><br>
--
<% end %>
</div>
</div>

</body>
</html>
